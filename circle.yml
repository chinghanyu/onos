machine:
  timezone:
    America/Los_Angeles
  java:
    version: oraclejdk8

dependencies:
  override:
    - mvn clean install

test:
  override:
    - echo "Skipped."

deployment:
  staging:
    branch: master
    heroku:
      appname: onos
